fun main() {

    // You can iterate over a String in Kotlin directly using a for-each loop:
    val brand = "marouacode"

    for (c in brand) println(c)

    // This is similar to Java but is more concise and easier to read:
    /*
    for (int i = 0; i < brand.length(); i++) {
        System.out.println(brand.charAt(i));
    }
    */
}
