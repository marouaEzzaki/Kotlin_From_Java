fun main() {
    val names = listOf(
        "Maroua",
        "Nadia",
        "Yasmina",
        "Fahd"
    )
    names.forEach {name -> println(name)}

    //the line above is the exact same
    //for (name in names) println(name)
}